name: Secrets test

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Secret echo
      run: |
        $variable = "${{ secrets.SamsPassword }}"
        write-host "password raw: ${{ secrets.SamsPassword }}"
        write-host "password processed: $variable"
